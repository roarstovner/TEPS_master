{"author":{"id":"0fb62b7075a4219f84b4bf8c9c235af1632f3fa4b63ca899f025ad8215a930fd"},"ops":[{"type":1,"timestamp":1778070874,"nonce":"pHTJmpv+q8VBfjasADJluDEjSaM=","title":"validate_masters() checks the schema (column names + types) but no value-level invariants. The Oslomet GFU_2024 omission would have been caught by a row-count regression test; the recurring `language = \"N/A\"` strings would be caught by a vocabulary check.","message":"- Add R/test_masters.R (or use testthat under tests/) covering:\n  - per-institution row count is within an expected band (e.g. ±5% of last commit's count, or hardcoded floor)\n  - no duplicated url among non-NA urls\n  - n_authors == count_entries(authors) when authors is non-NA\n  - GLU values are a subset of master_levels$master_program\n  - language values are a subset of an agreed vocabulary (or known free-text exceptions)\n  - year within plausible range (e.g. 2008..current_year + 1)\n  - id is unique (already true today; lock it in)\n- Wire the tests into data-raw/masters.R after the merge, before write_rds().\n- Decide whether failures should `stop()` or just warn — same mechanism as the validate_masters() halt-on-error bug.\n\nDepends on the validate_masters()-stops-pipeline bug: that one establishes the error-raising convention; this bug adds value-level checks on top of it. Should land after.","files":null}]}